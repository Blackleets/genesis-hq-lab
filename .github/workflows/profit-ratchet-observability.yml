name: Profit ratchet observability checks

on:
  push:
    branches: [feat/genesis-life-os]
  pull_request:
    branches: [main, feat/genesis-life-os]

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - run: npm ci --no-audit --no-fund
      - name: Ratchet invariants and economic observability
        run: node --test server/tests/futuresRunnerProfitCaptureContract.test.mjs server/tests/profitRatchetExitAudit.test.mjs
      - name: Production build
        run: npm run build
