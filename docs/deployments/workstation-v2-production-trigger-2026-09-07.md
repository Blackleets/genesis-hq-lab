# Genesis Workstation v2 — production deployment trigger

This file is an inert deployment marker only.

- Branch: `feat/genesis-life-os`
- Target: Vercel production project `genesis-hq-lab`
- Runtime/trading logic: unchanged
- Founder execution authority: unchanged
- LIVE state: unchanged / must remain locked

Purpose: trigger a single production build after Vercel build-rate-limit recovery so the already-reviewed Workstation v2 source can be published to the existing production domain.
